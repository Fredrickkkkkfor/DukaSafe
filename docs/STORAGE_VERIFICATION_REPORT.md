# Storage Verification Report

Last updated: 2026-07-01

## Live Script

`pnpm verify:rls` uploaded tiny PNG fixtures through signed-in buyer/seller/admin clients, then removed them.

| Bucket | Role Tested | Expected | Result |
|---|---|---|---|
| `seller-documents` | Seller | Seller uploads own ID document path | Pass |
| `shop-photos` | Seller | Seller uploads public shop photo | Pass |
| `product-images` | Seller | Seller uploads public product image | Pass |
| `payment-proofs` | Buyer | Buyer uploads own payment proof | Pass |
| `delivery-proofs` | Seller | Seller uploads own delivery proof | Pass |
| `dispute-evidence` | Buyer | Buyer uploads own dispute evidence | Pass |

## Access Checks

- Public could not read a private payment proof path.
- Admin could create a signed URL for a private payment proof.
- Public could read intended public `shop-photos` content.
- Private raw public URLs were not generated by the app for evidence/document buckets.

## Previous Manual Check

Earlier admin verification UI only displayed protected metadata. This is now addressed in the final edge closure update below with server-side signed preview links. Deployed staging verification is still required.

## Final Edge Closure Update

Date: 2026-07-01

Fix made:

- Added server-side signed evidence preview link generation.
- Admin seller verification now exposes protected seller document preview links through short-lived signed URLs.
- Admin dispute review now exposes protected payment proof, delivery proof, and dispute evidence preview links through short-lived signed URLs.

Security posture:

- Private buckets remain private.
- Raw public URLs were not introduced for private evidence buckets.
- Signed URLs are generated server-side from authenticated admin/operations pages.

Still required before launch:

- Verify signed links on the deployed staging URL with an admin account.
- Verify buyer/seller/public sessions cannot open private evidence they do not own or are not authorized to review.
- Verify signed URL expiry behavior is acceptable for admin operations.

Storage verdict:

Ready for controlled staging. Production launch still requires deployed evidence-preview QA and physical phone upload verification.
